# Tasks

- [x] 1. Add strict OpenSpec requirements and validate change.
- [x] 2. Add failing compiler test for Sketch typing and argument preservation.
- [x] 3. Add failing deterministic clipping tests: square grid, inset, ordering,
      duplicates, invalid index, collapsed cell.
- [x] 4. Implement shared exact Voronoi clipping kernel.
- [x] 5. Add surface export, Sketch inference, validation, native allowlist, and
      Direct OCCT expansion to `polygon`.
- [x] 6. Add explicit native-only interop diagnostics.
- [x] 7. Add native planning/render proof plus hybrid
      `solidify(import-stl(...))` boolean coverage.
- [ ] 8. Apply op to bottle cage, verify viewport, then run 2/3-bolt FEM.
- [x] 9. Run focused tests, `cargo check`, and strict OpenSpec validation.
