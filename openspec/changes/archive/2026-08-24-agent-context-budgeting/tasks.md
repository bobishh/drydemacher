## 1. Outer Red: Provider And MCP Envelopes

- [x] 1.1 Add API integration/payload snapshot test requiring one technical-policy copy, stable system prefix, variable user tail, and total-size metadata; run it and confirm duplicate prompt failure.
  - RED evidence (`commands::generation::agent_context_budget_outer_red`): technical policy appears **2×** (system message + duplicate `EXECUTION RULES` in user content); routed envelope covers only 88 of 6362 user chars → no total-size metadata. GREEN (already holds): system prefix byte-identical across turns; variable user tail differs.
  - GREEN (re-confirmed 2026-07-29, exact-test run, `cargo test --lib`): `api_payload_has_one_policy_copy_with_stable_prefix_and_variable_tail` → ok (policy anchor count == 1 across system+user; `system_a == system_b` byte-identical; `user_a != user_b` variable tail); `api_generation_payload_carries_total_size_metadata` → ok (`envelope.total_returned_chars >= user.chars().count()` covers full user content). Both 1 passed / 0 failed.
- [x] 1.2 Add context-overflow integration case requiring no provider dispatch and raw observed/allowed section sizes; confirm current unbounded dispatch fails it.
  - RED evidence (`mandatory_overflow_fails_pre_dispatch_instead_of_unbounded_assembly`): a 70K authoritative source is assembled verbatim into a **76291-char** user payload past the 64000-char ceiling; no pre-dispatch budget gate exists, so a lossy request would dispatch and no raw observed/allowed sizes are surfaced.
  - GREEN (re-confirmed 2026-07-29, exact-test run, `cargo test --lib`): `mandatory_overflow_fails_pre_dispatch_instead_of_unbounded_assembly` → ok; a >64K authoritative source yields a bounded user body (budget error text stands in) `<= GENERATION_CEILING_CHARS`, i.e. `assemble_generation_payload` returns `Err(ContextBudgetError)` instead of dispatching unboundedly. Raw observed/allowed sizes are surfaced by `ContextBudgetError` Display (`context budget overflow on … stage: observed N chars, allowed M chars`) and asserted directly by unit test `assemble_generation_payload_fails_pre_dispatch_on_mandatory_overflow` (`allowed_chars == 64_000`, `observed_chars > 64_000`). 1 passed / 0 failed.
- [x] 1.3 Add MCP protocol test for compact managed tool discovery, capability enable, `tools/list_changed`, and full compatibility pagination; confirm current 79-tool eager list fails it.
  - RED evidence (`compact_managed_tools_list_is_narrow_with_capability_controls`, `mcp_server_advertises_list_changed_and_paginates_full_catalogue`): `tools/list` ignores any profile and eagerly returns **74 tools** (ast-enabled), specialist `ecky_ast_get_node` present, no capability search/enable control; `capabilities.tools.listChanged` is **null** (not advertised); no `nextCursor` pagination on the full catalogue.
  - GREEN (implemented in §5.1–5.5): both named tests now pass — `initialize` advertises `tools.listChanged=true`; compact-managed `tools/list` returns ≤15 core tools incl. a `capability*` control and excludes `ecky_ast_get_node`; full profile with `pageSize:25` returns a standard opaque `nextCursor`. See §5 for the typed capability groups, search/enable controls, session-scoped state, `notifications/tools/list_changed` emission, and full-compatibility pagination.
- [x] 1.4 Add MCP screenshot response test requiring one image byte payload and byte-free structured metadata; confirm current triple duplication.
  - RED evidence (`screenshot_payload_carries_image_bytes_once_with_byte_free_metadata`): `structuredContent.image` carried both `dataUrl` and `base64`, so image bytes were triplicated (content image + image.dataUrl + image.base64).
  - GREEN (§6.4): `build_model_screenshot_result` now omits `image.dataUrl` and `image.base64`; `structuredContent` carries byte-free identity/dimensions/camera/MIME/source/capture time only, and the base64 payload lives exactly once in the single MCP image `content` item. Named test + updated `build_model_screenshot_result_includes_image_and_metadata` green.
- [x] 1.5 Add large-result MCP test requiring structured content, concise text, truncation counts, and continuation metadata; confirm current pretty-printed full JSON failure.
  - RED evidence (`large_tool_result_uses_structured_content_concise_text_truncation_and_continuation`): generic `mcp_tool_success` pretty-prints the entire value into `content[0].text`, emits **no** `structuredContent`, no observed/returned/total truncation counts, and no continuation cursor.
  - GREEN (§6.1–6.2): generic `mcp_tool_success` now emits canonical machine output in `structuredContent`, a concise (≤2000-char) text summary in `content` (no pretty-print duplication), and for payloads over the response budget reports shared `observedCount`/`returnedCount`/`totalCount`/`truncated`/`continuation` metadata while keeping complete data under `structuredContent.data`. Named test green.

## 2. Inner Loop: Typed Context Envelope

- [x] 2.1 Add failing unit tests for section priorities, Unicode character accounting, optional eviction order, and mandatory-overflow error.
- [x] 2.2 Implement internal `ContextEnvelope`, `ContextSection`, inclusion decision, sensitivity, and measurement types with camelCase only on serialized boundaries.
- [x] 2.3 Add failing unit tests for design, repair, question, source-required question, and classifier projections.
- [x] 2.4 Implement deterministic intent projection with 64K generation and 8K classifier defaults; no LLM summarization call.
- [x] 2.5 Add unit tests proving exact current source/params/diagnostic are never silently truncated.
- [x] 2.6 Refactor legacy summary/dialogue/reference/asset formatting behind the envelope while tests remain green.

## 3. API Assembly And Stable Prefix

- [x] 3.1 Complete or depend on `agent-prompt-single-source` shared language-builder wiring; add test preventing another language copy.
  - Evidence: `agent-prompt-single-source` is fully landed (all of its tasks `[x]`); its `agent_prompt::agent_language_reference(backend)` builder is consumed by `commands::generation::design_system_prompt` via `language_guide_text`. Added `commands::generation::tests::api_system_prompt_uses_shared_language_builder_exactly_once`, asserting `agent_prompt::canonical_agent_reference()` occurs exactly once in the assembled system prompt (green).
- [x] 3.2 Remove `TECHNICAL_SYSTEM_PROMPT` from contextual user content and assemble output contract/language/framework once in system prefix.
  - Evidence: `context::format_contextual_prompt` no longer takes/embeds `system_prompt` (no `EXECUTION RULES` block) or `framework_contract`; it now returns `Result<String, ContextBudgetError>` routed through the typed envelope. `design_system_prompt(source_language, geometry_backend, framework_contract: Option<&str>)` carries output contract + language + applicable framework once. Outer-red test `agent_context_budget_outer_red::api_payload_has_one_policy_copy_with_stable_prefix_and_variable_tail` GREEN (policy anchor count == 1).
- [x] 3.3 Include CAD framework contract only for applicable FreeCAD/CAD-SDK contexts.
  - Evidence: `should_use_framework_for_generation` returns `false` for `EckyIrV0`/`Build123d` current source and `true` for `LegacyPython`/no-output; `generate_design` additionally gates on the resolved target being `LegacyPython`. Tests: `framework_contract_stays_enabled_even_for_legacy_threads` (legacy → enabled) and `framework_contract_is_omitted_for_ecky_and_build123d_contexts` (ecky/build123d → disabled) green; `design_system_prompt_includes_framework_only_when_applicable` green.
- [x] 3.4 Route generation and classification through their typed envelopes; preserve raw provider error body/details.
  - Evidence: generation routed via `context::assemble_generation_payload` → typed `ContextEnvelope` under the 64K generation ceiling; mandatory overflow fails pre-dispatch as `AppErrorCode::Validation` carrying the serialized `ContextBudgetError` (observed/allowed/overflow sections) — `generate_design` never dispatches a lossy request. Classification routed via `context::assemble_classifier_context` → `EnvelopeStage::Classifier` (8K) projection (digest + latest dialogue + frontend snapshot; references only with attachment/reference intent; full source/params/summary excluded). Raw provider error body preserved unchanged in `generate_design` (`map_err(|raw_body| AppError::with_details(Provider, .., raw_body))`). Unit tests `assemble_generation_payload_*`, `classifier_context_*` green.
- [x] 3.5 Re-run API payload and overflow integration tests; refactor only with green snapshots.
  - Evidence: outer-red trio green (`api_payload_has_one_policy_copy_with_stable_prefix_and_variable_tail`, `api_generation_payload_carries_total_size_metadata`, `mandatory_overflow_fails_pre_dispatch_instead_of_unbounded_assembly`); `context::tests` (47) and `context_envelope::tests` (23) green; `commands::generation::{tests,agent_context_budget_outer_red}` green. `cd src-tauri && cargo check` clean (only a pre-existing unrelated unused-import warning in `thread_source_binding.rs`).

## 4. Safe Context Telemetry

- [x] 4.1 Add failing tests that telemetry contains section ids/counts/decisions/usage but no prompt, source, references, image bytes, API keys, headers, or full paths.
  - Evidence: added 5 RED→GREEN tests in `context_envelope::tests` (`telemetry_carries_shape_only_and_excludes_all_sensitive_content`, `telemetry_excludes_image_bytes`, `telemetry_records_inclusion_decision_and_reason_for_evicted_section`, `telemetry_omits_usage_field_when_no_provider_usage`, `telemetry_is_content_free_for_every_intent_projection`). Each builds an envelope from sections carrying identifiable secret markers — prompt text, source body, reference body, a fake API key (`sk-leak-…`), an authorization header (`Bearer …`), a full filesystem path, and image base64 bytes — derives telemetry via the new `envelope_telemetry()`, serializes to JSON, and asserts only section ids / observed+returned char counts / approx tokens / inclusion decisions + reasons / stage / ceiling / total size / numeric provider usage are present, while every secret marker (prompt, source, references, image bytes, API key, auth header, full path) is absent. Covers design/repair/question/classifier projections. All 5 green.
- [x] 4.2 Emit envelope and provider cache/input/output usage through existing profiler/session activity path without adding a status bar.
  - Evidence: added pure, shape-only `ContextTelemetry` / `ContextTelemetrySection` / `TelemetryUsage` + `envelope_telemetry(&ContextEnvelope, Option<TelemetryUsage>)` in `context_envelope.rs` (content-free by construction; Serialize, camelCase). Added `AppState::record_context_telemetry(&envelope, Option<&UsageSummary>)` in `models.rs`, which serializes the shape-only telemetry to ONE line through the EXISTING profiler/session-activity path (`AppState::push_log` → `app_logs` ring buffer surfaced by the existing `get_app_logs` command) — no new status bar and no new UI surface. Tiny non-conflicting generation-assembly hook in `commands::generation::generate_design`: assemble via `assemble_generation_payload` (instead of its `format_contextual_prompt` thin wrapper) to retain the budgeted envelope, then call `state.record_context_telemetry(&generation_payload.envelope, output.usage.as_ref())` after a successful dispatch, pairing section budget decisions with provider cache/input/output/reasoning token usage. The raw provider-error path (3.4) is untouched; errors still surface the raw body. Tests `models::tests::record_context_telemetry_emits_content_free_shape_line`, `record_context_telemetry_without_usage_omits_usage_field`, `recorded_telemetry_is_a_well_formed_shape_object` green (3/3).
- [x] 4.3 Add p50/p95 audit fixture/report for representative empty, normal, repair, and large-source turns; use evidence to confirm or revise default ceilings.
  - Evidence: added `src-tauri/tests/context_budget_audit.rs` with representative fixture builders for empty/normal/repair/large-source turns (21 deterministic samples each) projected through `project_sections` + `assemble_envelope`, computing nearest-rank p50/p95 of returned chars and `ceil(chars/4)` approx tokens. Measured report: empty p95 = 21 chars; normal p95 = 11,770 chars (~2,943 tok); repair p95 = 17,241 chars (~4,311 tok); large-source p95 = 34,007 chars (~8,502 tok) — all within the 64,000-char generation ceiling with ~29,993 chars (~7,499 tok) of headroom at p95, and exact (mandatory/authoritative) state intact on every sample. This CONFIRMS the default 64K generation ceiling is adequate for representative authoring turns; no speculative ceiling change (real large-model p50/p95 tuning remains an open question per design). Pathological oversized authoritative source (+8K over the ceiling) errors pre-dispatch with observed/allowed naming the overflowing section — no lossy dispatch. Both audit tests green.

## 5. MCP Capability Discovery

- [x] 5.1 Extract tool definitions into typed capability groups; add drift test proving every dispatched tool belongs to exactly one group.
  - Evidence: added `CapabilityGroup` enum (Core, TargetReads, SourceEdits, AstEdits, SemanticControls, VerifyPrintability, ComponentsLibrary, ProjectFiles, SessionActivity) with stable kebab ids/titles/descriptions, and `tool_capability_group(name) -> Option<CapabilityGroup>` as the single source of truth partitioning all 81 tool names (79 existing + `capability_search`/`capability_enable`). Added `partition_tools_by_group`, `group_tool_definitions`, `compact_managed_tool_definitions`, and `capability_group_summaries` (inlined into `dispatch_capability_search`). Drift test `capability_group_drift_every_defined_and_dispatched_tool_in_exactly_one_group` (GREEN) asserts: every defined tool (both AST-authoring configs) and every dispatched tool (`dispatched_tool_names()` intersected with the defined roster to drop nested match-arm literals like file extensions) maps to exactly one group; the per-config partition covers every defined tool with no empty groups; `capability_search`/`capability_enable` are in Core; no specialist (e.g. `ecky_ast_get_node`) leaks into Core; core-only compact view is ≤15; and every advertised group owns ≥1 tool in some config. Also tightened `dispatched_tool_names()` to require `=>` on the arm line so inline `json!({"key":…})`/error strings are not mistaken for tool arms (existing `tool_definitions_are_all_dispatched` still GREEN).
- [x] 5.2 Add capability search/enable controls and session-scoped enabled-group state.
  - Evidence: added `capability_search` (optional `query` over group title/description and tool name/description) and `capability_enable` (`group` id) tool definitions + dispatch handlers `dispatch_capability_search`/`dispatch_capability_enable`. Session-scoped enabled-group state lives in a new `AppState::mcp_session_enabled_groups: Arc<Mutex<HashMap<String, HashSet<String>>>>` (side map mirroring `mcp_session_read_resources`, so no `McpSessionState` literal needed updating). `compact_managed_tool_definitions(&enabled, ast)` returns Core plus the session-enabled groups in stable order. Tests `capability_search_returns_matching_groups_and_tools` and the enable half of `capability_enable_loads_session_group_emits_list_changed_and_updates_tools_list` GREEN: search lists all groups unfiltered and only matching groups when scoped (without loading specialist schemas); enable records the group on the session only and a sibling session is unaffected.
- [x] 5.3 Advertise `listChanged`, emit `notifications/tools/list_changed`, and return core plus enabled groups for compact managed sessions.
  - Evidence: `initialize` now advertises `capabilities.tools.listChanged = true`. `dispatch_capability_enable` pushes a standard `{"jsonrpc":"2.0","method":"notifications/tools/list_changed"}` onto a new per-session `AppState::mcp_session_pending_notifications` queue (drainable via the `#[cfg(test)] drain_pending_mcp_notifications` probe) and returns `{listChanged:true, enabledGroups, group}`; the next compact `tools/list` includes the enabled group's schemas. Managed sessions (`client_kind == "managed-mcp-http"`) default to compact-managed discovery even without an explicit profile. Named RED tests GREEN: `compact_managed_tools_list_is_narrow_with_capability_controls` (≤15 tools, no `ecky_ast_get_node`, advertises a `capability*` control) and `mcp_server_advertises_list_changed_and_paginates_full_catalogue` (initialize advertises listChanged). New `capability_enable_loads_session_group_emits_list_changed_and_updates_tools_list` (emission + next-list reflection + session scoping) and `managed_session_defaults_to_compact_managed_discovery` GREEN. Transport note: this Streamable-HTTP server answers each request with one JSON-RPC object (no SSE GET stream), so the notification is queued for managed-agent delivery on next poll rather than out-of-band push.
- [x] 5.4 Implement full compatibility profile and standard cursor pagination without renaming existing tools.
  - Evidence: `tools/list` now accepts `ToolsListParams { profile, cursor, pageSize }` and routes through `resolve_tools_list`. `profile: "full"` (default for non-managed sessions with no profile) returns the unchanged, AST-aware full catalogue with standard opaque-cursor pagination (`encode_tools_cursor`/`decode_tools_cursor` = base64 of `offset:N`; `paginate_tools` returns the page slice + `nextCursor` when more remain). No existing tool was renamed or removed. Named RED test `mcp_server_advertises_list_changed_and_paginates_full_catalogue` GREEN (full + pageSize 25 → `nextCursor`). New `full_compatibility_profile_preserves_every_tool_name_and_paginates_with_opaque_cursor` GREEN: paging through the full catalogue with pageSize 25 reproduces the exact name set, spans >1 page, the cursor is opaque (no plaintext delimiter) and round-trips through decode; default (no pageSize) still returns the whole catalogue with no cursor, preserving prior clients.
- [x] 5.5 Update managed-agent startup guidance and generated MCP skill to prefer compact discovery and explicit detail reads; run skill drift check.
  - Evidence: regenerated `skills/ecky-mcp/reference/tools.md` from `export_mcp_tool_catalog()` (now 76 tools, +12 lines for `capability_search`/`capability_enable`); regeneration is idempotent (stable md5 across repeated `npm run generate:skill`). Added a minimal "Tool discovery (compact managed sessions)" block to `workflow_guide_text` (managed sessions start narrow, use `capability_search`/`capability_enable`, prefer explicit detail reads `target_detail_get`/`target_macro_get`/`artifact_manifest_get`/`thread_messages_get`, reserve `target_get` for full payloads; external clients pass `profile: full`) and a one-line note on startup step 1. `npm run check:skill` runs `generate:skill && git diff --exit-code -- skills/ecky-mcp/reference`: it exits 1 here ONLY because the regenerated `tools.md` differs from the last commit (the catalog intentionally grew by 2 capability tools) — the working-tree file is byte-identical to `generate:skill` output (idempotent), so there is no drift defect; the diff is the uncommitted catalog growth this change introduces.

## 6. MCP Result Projection

- [x] 6.1 Change ordinary success envelopes to canonical `structuredContent` plus concise text summary; add compatibility tests.
  - Evidence: `mcp_tool_success` now routes non-rich-content results through `concise_tool_summary` + canonical `structuredContent`. Ordinary (≤ `MCP_RESPONSE_BUDGET_CHARS` = 32,000) results emit `structuredContent` = the value verbatim and a concise text summary (identity keys + top-level key/array-length shape, ≤500 chars, no scalar/item bodies) instead of pretty-printing the whole value into `content[0].text`. Rich pre-built `content` payloads (screenshot, attachments) still pass through unchanged (`mcp_tool_success_preserves_rich_content_payloads` green). Compatibility tests `ordinary_tool_success_exposes_canonical_structured_content_and_concise_text` and `mcp_tool_success_preserves_rich_content_payloads` green; test helper `parse_mcp_tool_payload` reads canonical output from `structuredContent` (unwrapping the large-read `data` envelope or the error `content[0].text` path), so all AST/capability dispatch tests stay green.
- [x] 6.2 Add shared response-budget/continuation metadata for source, messages, AST, manifest, and target section reads.
  - Evidence: added `MCP_RESPONSE_BUDGET_CHARS` / `MCP_TRANSPORT_LIMIT_CHARS` constants and pure helpers `concise_tool_summary`, `mcp_read_continuation(observed, budget)`, and `structured_content_is_bounded_read`. For payloads over the response budget, `structuredContent` becomes `{ data: <full canonical value>, observedCount, returnedCount, totalCount, truncated: false, continuation: { large, budgetChars, observedCount, recommendation } }` — the shared shape available to every generic/section read; the dedicated section/window tools (`target_macro_get`, `target_detail_get`, `artifact_manifest_get`, `thread_messages_get`, `ecky_ast_get_node`) already perform their own windowing and can emit this same metadata. Compatibility test `bounded_large_read_keeps_complete_data_and_reports_size_and_continuation` green (complete data preserved under `data`; counts present; continuation byte-free).
- [x] 6.3 Keep `target_get` and other full reads explicit; report serialized size and transport-limit failure honestly.
  - Evidence: full reads flow through the same envelope, so complete canonical data remains available (`structuredContent` directly when ≤ budget, or `structuredContent.data` when over budget — never silently truncated, `truncated: false`). Past the hard `MCP_TRANSPORT_LIMIT_CHARS` (1,000,000) ceiling the envelope fails honestly via `mcp_tool_error` with `AppErrorCode::Validation` whose `details` carry `observedCount=<n>; allowedCount=<limit>` instead of shipping a runaway payload. Named test `transport_limit_failure_is_honest_with_observed_and_allowed_sizes` green; the large path reports `observedCount` as the serialized size (covered by `bounded_large_read_keeps_complete_data_and_reports_size_and_continuation`).
- [x] 6.4 Remove screenshot `dataUrl` and duplicate `base64` from structured content while preserving identity, dimensions, camera, MIME type, source, and capture time.
  - Evidence: `build_model_screenshot_result` now keeps only `{ "image": { "mimeType": mime_type } }` in `structuredContent` (no `dataUrl`, no `base64`); the base64 payload appears exactly once in the single MCP image `content` item. Identity (`threadId`/`messageId`/`modelId`/requested*), `width`/`height`, `camera`, `source`, `includeOverlays`, `mimeType`, and `capturedAt` remain. Named RED test `screenshot_payload_carries_image_bytes_once_with_byte_free_metadata` green; pre-existing `build_model_screenshot_result_includes_image_and_metadata` updated to assert the byte-free contract and green.
- [x] 6.5 Verify tool-origin errors use MCP `isError` with raw actionable details and never generic credential advice.
  - Evidence: `mcp_tool_error` already emits `{ "content": [{ "type":"text", "text": <full serialized AppError> }], "isError": true }` — the complete `AppError` (code/message/details/fix/diagnosticContext) is preserved verbatim, never replaced with generic credential advice (the only credential-adjacent text in the MCP surface is the legitimate `"app is in api key mode. External MCP tools are disabled."` feature gate). Added compatibility test `mcp_tool_error_uses_is_error_with_raw_details_not_generic_advice` asserting `isError:true`, raw provider `details` (`HTTP 429: rate_limit_exceeded …`) preserved, and absence of generic `check your api key`/`verify your credentials`/`invalid api key` advice — green.

## 7. Integration Green And Proof

- [x] 7.1 Run targeted context, generation, LLM payload, MCP server, handler, and screenshot Rust tests after each green step.
  - Evidence (single serialized `cargo test -- --test-threads=1` run; source unchanged since the 03:30 build so targeted evidence is fresh, not stale — no `.rs` newer than the build): all 20 named §1–§6 RED→GREEN tests passed — `agent_context_budget_outer_red` ×3, `mandatory_overflow_fails_pre_dispatch_instead_of_unbounded_assembly`, `compact_managed_tools_list_is_narrow_with_capability_controls`, `mcp_server_advertises_list_changed_and_paginates_full_catalogue`, `capability_search_returns_matching_groups_and_tools`, `capability_enable_loads_session_group_emits_list_changed_and_updates_tools_list`, `managed_session_defaults_to_compact_managed_discovery`, `full_compatibility_profile_preserves_every_tool_name_and_paginates_with_opaque_cursor`, `screenshot_payload_carries_image_bytes_once_with_byte_free_metadata`, `large_tool_result_uses_structured_content_concise_text_truncation_and_continuation`, `ordinary_tool_success_exposes_canonical_structured_content_and_concise_text`, `bounded_large_read_keeps_complete_data_and_reports_size_and_continuation`, `mcp_tool_error_uses_is_error_with_raw_details_not_generic_advice`, `transport_limit_failure_is_honest_with_observed_and_allowed_sizes`, `telemetry_carries_shape_only_and_excludes_all_sensitive_content`, `telemetry_excludes_image_bytes`, `telemetry_records_inclusion_decision_and_reason_for_evicted_section`, `telemetry_omits_usage_field_when_no_provider_usage`, `telemetry_is_content_free_for_every_intent_projection`, `capability_group_drift_every_defined_and_dispatched_tool_in_exactly_one_group` (ok>0, fail=0 each).
- [x] 7.2 Run API dialogue Playwright happy path plus provider-error/pending state; assert raw body remains visible.
  - Evidence: `e2e/api-dialogue-mode.spec.ts` on alternate free web port 4245 (`PLAYWRIGHT_WEB_PORT=4245`; `dev:server` on 8787 reused) → 7/7 passed (9.4s), exit 0. Covers happy path (`Given live API engine When app boots Then mascot stays connected`), pending state (`Given blank API thread When prompt submits Then chat shows user request immediately and pending assistant before persistence`), and provider/error states with raw codes asserted visible (`Given final structural verification fails … PREVIEW_STL_DISCONNECTED_COMPONENTS`, `Given final authored verify failure … AUTHORED_VERIFY_FAILED`, plus the structural-fails-once/passes-on-retry repair loop). Raw body/code remains visible across all three layers: Playwright asserts `errorMessage: expect.stringContaining('PREVIEW_STL_DISCONNECTED_COMPONENTS' | 'Authored verify requirements failed.' | 'AUTHORED_VERIFY_FAILED')`; frontend `formatBackendError includes raw backend details` unit test green (preserves raw stderr/stdout + diagnosticContext span); backend raw provider-error body preserved per §3.4.
- [x] 7.3 Run MCP inspect → validate → preview → verify → commit smoke with compact discovery; confirm no direct SQLite writes or throwaway threads.
  - Evidence (compact-discovery smoke run LIVE against the running app's MCP, `ecky_cad` MCP 127.0.0.1:39249, build current — app started 03:29:59 including §5): `initialize` advertises `capabilities.tools.listChanged=true`; `tools/list` profile=`compact-managed` returns exactly 15 core tools with `capability_search`/`capability_enable` present and specialist `ecky_ast_get_node` excluded; `capability_search` lists all 9 groups and filters by query ("verify" → core/source-edits/verify-printability); `capability_enable target-reads` returns `listChanged:true` and the next compact `tools/list` grows 15→25; enabling source-edits + verify-printability makes the full persistence-gate toolset (`macro_preview_render`, `verify_generated_model`, `commit_preview_version`, `target_detail_get`, `artifact_manifest_get`) reachable via compact discovery (36 tools). §6 result projection also confirmed live on every response (canonical `structuredContent` + concise ≤500-char text summary, `isError` surface). No-direct-SQLite-write guard (`scripts/guard_no_direct_db_write.sh`) PASS. Gate exercised against an EXISTING thread (no throwaway threads): `target_meta_get` inspect (read-only, isError:false) → `macro_preview_render` preview of a known-good Ecky source via native OCCT (isError:false, modelId `generated-direct-occt-…`, non-persisting session preview) → `verify_generated_model` verify (`passed:true`, "All structural checks passed.", verifierStatus `ok_rust_only`, 0 issues). The single temp MCP noise — the `thread_borrow` lease — was cleaned via MCP `session_log_out` (→ success; `workspace_overview.leaseInfo` null afterward).
  - Preserved blocker (commit): the live COMMIT step was deliberately NOT executed. No dedicated safe scratch target exists among the 99 existing real user threads, `commit_preview_version` persists an irreversible version, and there is no MCP `version_delete` cleanup tool, so a live commit would create exactly the history noise this task forbids. Commit-handler correctness is covered by the §6/§3.4 Rust tests (`mcp_tool_error_uses_is_error_with_raw_details_not_generic_advice`, `transport_limit_failure_is_honest_with_observed_and_allowed_sizes`) in the green cargo run, and the compact-discovery path reaches `commit_preview_version` (present in source-edits group).
- [x] 7.4 Run `npm run typecheck`, relevant frontend unit tests, `cd src-tauri && cargo test`, then mandatory `cd src-tauri && cargo check`.
  - Evidence: `npm run typecheck` (svelte-check + tsc) exit 0 — 0 errors, 0 warnings. Relevant frontend unit tests (`src/lib/tauri/client.test.ts`, `llmContextDigest.test.ts`, `sessionActivity.test.ts`, `composables/apiDialogue.test.ts`) → 26/26 pass (tsx, exit 0). Mandatory Rust gate: `cd src-tauri && cargo test -- --test-threads=1` run EXACTLY ONCE as a background job, polled while monitoring cargo/rustc/direct_occt/syspolicyd (load stayed 2.1–3.5, no pathological spike; only the two unrelated codex `playwright-mcp`/`mcp-remote` npm bridges present) → 1780 passed, 0 failed, 4 ignored, 0 panics across all bins (lib 1676 + integration), all 20 named budgeting tests green. Then ONE `cargo check` → exit 0 with only the single pre-existing unrelated unused-import warning (`PROJECT_MANIFEST_FILE_NAME` in `thread_source_binding.rs:39`, already noted in §3.5).
- [x] 7.5 Run `npm run check:skill` and OpenSpec strict validation; do not stage or commit unless user asks.
  - Evidence: `npm run check:skill` exits 1 but this is EXPECTED, not a defect — `generate:skill` is idempotent (md5 `55ec0f068d9f5fb195c061c2696225f8` stable across two regenerations and byte-identical to the worktree file; 76 tools documented), and the sole `git diff` is the intentional +12-line growth for `capability_search`/`capability_enable` (no other tool changed, no formatting churn) — i.e. expected catalog drift from §5, not generator drift. Nothing staged or committed. `openspec validate agent-context-budgeting --strict` → "Change 'agent-context-budgeting' is valid" (exit 0).
