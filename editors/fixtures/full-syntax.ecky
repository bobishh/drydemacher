; Ecky CAD DSL - Full Syntax Fixture File
; This file tests all canonical syntax elements for editor support

; Comments
; This is a single-line comment
; Another comment with special chars: +-*/<>=?!

; Model clauses
(model
  (params
    (number radius 20 :label "Radius" :min 5 :max 80)
    (number height 30 :label "Height" :min 10 :max 100)
    (boolean useCap #f :label "Use cap"))
  (part body (cylinder radius height 48))
  (meta :title "Test Model" :version "1.0")
  (verify (tag body) (metric body (stl connected-component-count)) (expect body (= 1)))
  (feature shell :role enclosure :params (radius) (box radius 40 2))
  (tag-vertex datum :vertex "top" body)
  (tag-face mounting :faces "bottom" body)
  (tag-edge rim :edges "top" body)
  (tag-edges rim-pair :edges "top" body)
  (view print-layout (offset-part body 0 0 0))
  (analysis load-case (linear-static :part body) (solve :method direct)))

; Model wrappers
(begin
  (params (number x 10))
  (part base (box x x x)))

(let ((r 20) (h 40))
  (part cylinder-part (cylinder r h 32)))

(let* ((r 10) (h (* r 3)) (wall 2))
  (part tube (difference (cylinder r h 32)
                          (cylinder (- r wall) h 32))))

; Expression forms
(define wall-thickness 2)

(define-component mounting-bracket
  (params (number width 40))
  (part bracket (box width width 10)))

(define make-box
  (lambda (size)
    (box size size size)))

(if useCap
    (sphere radius)
    (cylinder radius height 48))

; Quoted expression
(quote (center center min))

; Alternative quote syntax
'(center center min)

(list x y z)

(append front-points back-points)

(reverse points)

(range 8)

(map (lambda (i) (* i 10)) (range 4))

(filter (lambda (i) (even? i)) (range 8))

(fold + 0 (range 5))

(reduce * 1 numbers)

(zip xs ys)

(enumerate (range 4))

(linspace 0 360 12)

(flat-map (lambda (i) (list i (- i))) (range 3))

(concat-map (lambda (p) (list p)) points)

(apply union cutters)

; Numeric helpers
(+ width clearance)

(- outer inner)

(* radius 2)

(/ width 2)

(min wall max-wall)

(max wall 1.2)

(abs offset)

(floor segments)

(sin (deg->rad 45))

(cos (deg->rad 45))

(tan (deg->rad 45))

(atan slope)

(atan2 y x)

(deg angle-rad)

(rad 90)

(deg->rad 90)

(rad->deg pi-angle)

(clamp depth 0 3)

(lerp 10 20 0.25)

(smoothstep 0 1 t)

(hash01 ix iy seed)

(hash-signed ix iy seed)

(noise2 (* x 0.1) (* y 0.1) seed)

(fbm2 x y seed 4 2.0 0.5)

(voronoi2 (* x 0.15) (* y 0.15) seed)

(cell-distance2 x y seed)

(pi)

(tau)

(invlerp 0 1 0.5)

(remap 0 1 10 20 0.5)

(square 3)

(cube 2)

; Point list helpers
(vec2 x y)

(vec3 x y z)

(jitter2 10 20 2 seed)

(jittered-grid 4 6 12 12 2 seed)

(polar-points 32 20)

(organic-loop 32 30 4 seed)

(wave-loop 48 20 3 5 0)

(superellipse-point (deg->rad 45) 30 20 4)

(voronoi-cells 4 6 14 12 2 seed)

(lorenz-points 80 0.01 4)

(rossler-points 80 0.03 6)

(logistic-bifurcation-points 24 8 16 30)

(henon-points 100 12)

; Boolean helpers
(not value)

(and a b c)

(or x y z)

(= a b)

(> a b)

(>= a b)

(< a b)

(<= a b)

(even? number)

(odd? number)

(zero? number)

(null? value)

(empty? list)

(list? value)

; CAD operations - primitives
(box 40 20 10 :align '(min center min))

(sphere 12)

(cylinder 8 30 48)

(cone 12 6 30 48)

(circle 20 64)

(ring 20 10 64)

(rectangle 40 20)

(rounded-rect 40 20 3)

(rounded-polygon points 2)

(polygon ((0 0) (40 0) (40 20) (0 20)))

(profile :outer (circle 20) :holes (circle 6))

(make-face (polygon points))

(text "A" 12)

(svg iconData)

(import-stl "/tmp/part.stl")

; CAD operations - paths
(path (polyline points))

(polyline ((0 0) (10 0) (10 5)))

(bezier-path points)

(bspline points :closed #t)

; CAD operations - solid modifiers
(extrude (polygon points) 8)

(revolve profile 360)

(loft bottom top)

(sweep (circle 2 16) rail)

(helical-ridge :radius 32 :pitch 5.25 :height 16.8 :base-width 1.45 :crest-width 0.55 :depth 1.5)

(thread :radius 8 :pitch 2 :length 16 :depth 1)

(tapped-hole :iso "M8" :length 14)

(rib (box 20 20 20) (circle 3) (path (0 0 0) (0 0 30)))

(groove (box 20 20 20) (circle 3) (path (0 0 0) (0 0 30)))

(torus 20 5)

(ellipse 10 4)

(regular-polygon 6 10)

(trapezoid 20 10 8 :skew 3)

(wedge 20 10 20 5 5 15 15)

(slot-overall 40 10)

(slot-center-to-center 30 10)

(slot-center-point 0 0 20 0 10)

(slot-arc 20 0 90 10)

; CAD operations - modifiers
(shell 2 :faces "target-id:body:face:0-0-20:1256.637" (cylinder 20 80))

(offset 2 profile)

(offset-rounded 2 profile)

(fillet 2 :edges "x-min+z-max" body)

(chamfer 1 :edges "bottom" body)

(taper 30 0.7 0.7 (circle 12 32))

(twist 40 90 profile)

; CAD operations - booleans
(union a b c)

(fuse a b c)

(difference body hole)

(cut body hole)

(intersection a b)

(common a b)

(xor a b)

(compound body bolts)

; CAD operations - transforms
(translate 10 0 0 body)

(rotate 0 0 45 body)

(scale 1 1 0.5 body)

(mirror "x" 0 body)

; CAD operations - arrays
(linear-array 4 12 0 0 rib)

(radial-array 12 30 spoke)

(grid-array 3 5 12 12 hole)

(arc-array 8 30 0 180 notch)

(repeat 6 rib)

(repeat-union 6 rib)

(repeat-compound 6 rib)

(repeat-pick 6 rib)

(for-union (range 6) (lambda (i) ...))

(for-compound points (lambda (p) ...))

; CAD operations - frames and placement
(plane :origin '(80 0 6) :normal '(0 0 1))

(location (plane :origin '(80 0 6)) :rotate '(0 90 0))

(path-frame rail :at end :up '(0 0 1))

(place end-frame (cylinder 4 18) :offset '(0 0 -9))

(clip-box body :x '(0 100) :y '(-30 30) :z '(0 40))

(draft 2 body)

(clip-plane body :origin '(0 0 10) :normal '(0 0 1) :keep positive)

; CAD operations - build
(build (shape body) (result body))

(shape body)

(result body)

; CAD operations - special
(sampled-radial-loft (theta z fz) :height 40 :z-steps 24 :theta-steps 72 :radius (+ 18 (* 2 (sin (+ (* theta 6) (* fz 3.141592653589793))))))

; EckyRust-only operations
(mesh :vertices ((0 0 0) (10 0 0) (0 10 0)) :triangles ((0 1 2)))

(polyhedron :vertices ((0 0 0) (10 0 0) (0 10 0) (0 0 10)) :triangles ((0 2 1) (0 1 3) (1 2 3) (2 0 3)))

(protrude image-path 4 :width 100 :depth 70 :foreground dark)

(wall-pattern (:mode gyroid :depth 0.6 :uFreq 4 :vFreq 5 :seed 7) (shell 2 (cylinder 20 80)))

(hull (sphere 6) (translate 30 0 0 (sphere 6)))

(import-step "/absolute/path/component.step")

(voronoi-cell (voronoi-cells 3 3 12 12 1.5 23) 4 40 40 1.2)

; Mesh/EckyRust surface trim operations
(surface-trim
  (import-stl "/tmp/mesh.stl")
  :loop ((mesh-anchor 0 1 0 0) (mesh-anchor 1 0 1 0) (mesh-anchor 2 0 0 1))
  :keep-seed (mesh-anchor 3 1 0 0))

; Wall pattern modes (as values in wall-pattern)
(wall-pattern (:mode ribs :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode rings :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode spiral :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode diamond :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode hammered :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode fourier :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode cellular :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode fbm :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode gyroid :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode schwarz-p :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode schwarz-d :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode diamond-field :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode neovius :depth 0.5 :uFreq 8 :vFreq 4) target)
(wall-pattern (:mode attractor-field :depth 0.5 :uFreq 8 :vFreq 4) target)

; Keywords with colon
:label
:mode
:min
:max
:height
:width
:depth
:radius
:align

; Required symbols for testing
(model
  (params (number r 20))
  (part box-part (box 10 10 10))
  (part repeated (repeat 4 (box 5 5 5)))
  (part wall-pattern-part (wall-pattern (:mode gyroid :depth 0.6) (sphere 10)))
  (part radial-loft-part (sampled-radial-loft (theta z fz) :height 20 :z-steps 12 :theta-steps 36 :radius 10)))

; Definition highlighting test
(define my-box (box 10 10 10))
(define my-cylinder (cylinder 5 20 32))

; Part definition highlighting
(part main-body (union (box 20 20 20) (sphere 10)))

; Numbers
42
3.14
-10
-2.5
0
1.0
1e5
-1.5e-3

; Booleans
#t
#f

; Strings
"Hello, Ecky!"
"Path/to/file.stl"

; End of fixture
